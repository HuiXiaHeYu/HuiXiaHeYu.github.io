---
title: 目标检测
createTime: 2024/11/18 21:05:41
permalink: /article/141x5n20/
---

## 概述

> 检测图片中所有物体(模型关注的物体)：
>
> 1. 类别标签(Category Label)；
> 2. 位置(最小外接矩形/Bounding Box)

单目标任务：

1. 分类
2. 分类&定位

多目标任务：

1. 目标检测/物体检测
2. 图像分割/实例分割

### 传统方法

一般使用滑动窗口的框架，主要包括三个步骤：

1. 利用不同尺寸的滑动窗口框出图中的某一部分作为候选区域
2. 提取候选区域相关的视觉特征。比如人脸检测常用的Harr特征；行人检测常用的HOG特征等
3. 利用分类器进行识别，比如常用的SVM模型

在传统目标检测算法中，多尺度形变部件模型DPM的效果相对是比较优秀的；在DPM中将物体看成是多个不同组件的结合，其效果不错，但是检测速度相对比较慢

### 主流方法

基于深度学习模型，分为两类：

- two-stage(R-CNN、SPPNET、Fast R-CNN、Faster R-CNN等)：
  1. 产生候选区域(region proposals)
  2. 对候选区域进行分类(一般需要进行位置精修)
- one-stage(SSD、YOLO、FPN等)：
  1. 端到端，直接进行区域定位与分类

### 评价标准

#### IOU

`预测和真实bounding box的重叠度，两个bounding box重叠区域的面积占总面积的比值`
$$
IOU = \frac{A\cap B}{A\cup B}=\frac{S_{_{A,B}}}{S_{_A}+S_B-S_{_{A,B}}}
$$
![image-20250105182008063](./%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B.assets/image-20250105182008063.png)

值越大（接近1），重叠度越高，精度越大

#### MAP

- TP：实际为正例，预测为正例
- FP：实际为负例，预测为正例
- TN：实际为负例，预测为负例
- FN：实际为正例，预测为负例
- 精度（查准率）：

$$
precision = \frac{TP}{TP + FP}
$$

- 召回率（查全率）：

$$
recall = \frac{TP}{TP + FN}
$$

![image-20250105182442163](./%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B.assets/image-20250105182442163.png)

### 非极大值抑制

`目标检测中常用精修 bounding box的方式，抑制不是极大值的元素`

![image-20250105185055905](./%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B.assets/image-20250105185055905.png)

**标准计算方法**

对于CNN网络提取出来的Bounding Box列表B以及其对应的置信度S，采用下列执行过程来进行NMS操作：

- 从S和B中选择最大置信度score(至少大于0)以及对应Bounding Box框M，将其从B/S集合中移除，并将M加入到最终检测结果集合D中。
- 计算B中剩下所有的Bounding Boxi和M之间的IOU值，如果IOU值大于阈值t，那么表示这两个框比较相似，则将其从集合B中移除，否则不进行任何操作。
- 重复上述两个操作，直到集合B为空。

NOTE：上述操作针对每个类别进行操作。

**多目标检测**

对于多检测任务，如果对每个物体分别进行NMS，那么`当检测结果中包含两个被分到不同的目标且IOU较大的时候，会导致结果合并`，出
现不可接受的结果。

针对这个问题有两种解决方案：

1. 在损失函数中加入NMS损失函数
2. Soft-NMS：在重叠区域比较大的两个框中对于置信度较低值的框，不进行删除操作，而是进行置信度更新操作

## RCNN

1. 区域提名：通过此方法从原始图片中提取2000个左右的区域候选框，一般选择Selective Search(SS)
2. 区域大小归一化：将所有候选框大小缩放固定大小(eg:227*227)
3. 特征提取：通过CNN网络，对每个候选区域框提取高阶特征，将高阶特征保存磁盘
4. 分类与回归：在高阶特征的基础上添加两个全连接层，再使用SVM分类器进行识别，用线性回归来微调边框位置和大小，其中每个类别单独训练一个边框回归器(类似OverFeat的class specific结构)

<img src="./%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B.assets/image-20250106170219841.png" alt="image-20250106170219841" style="zoom:67%;" />

## SPPNET

RCNN的问题：

- **计算量大，速度慢**：每张图像的候选框均需要CNN提取高阶特征、SVM分类操作
- **图像失真**：每个候选框区域在输入CNN网络之前，需要进行warp拉伸/crop压缩操作，可能影响图像质量以及模型效果

SPPNET的改进：

- 直接送入整张图象，所有区域**共享卷积计算（一遍）**
  - 在Conv5层提取所有区域的高阶特征
- 引入空间金字塔池（Spatial Pyramid Pooling）
  - 为不同尺寸的区域，在Conv5层上提取不同的特征，然后映射到尺寸固定的全连接上

<img src="./%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B.assets/image-20250107153514039.png" alt="image-20250107153514039" style="zoom:67%;" />

## Faster RCNN

Faster RCNN=Fast RCNN + RPN

使用RPN网络取代Selective Search模块，解决Fast RCNN性能瓶颈

- 候选框生成网络RPN和检测网络Fast RCNN共享俊基层计算

Region proposals量少质优(~300)

- 利用NMS去除低质量的候选框
- 高precision、高recall

<img src="./%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B.assets/image-20250108161133322.png" alt="image-20250108161133322" style="zoom:67%;" />
